#!/bin/sh

if [ -z $2 ]
then
	if [[ "$1" =~ ^.*i\.redd.*$ ]]
	then
	file=~/.local/share/newsboatimg
	
	curl -s --output - "$1" > $file && sxiv -b  $file
	
	else
		mpv $1
	fi
else
	file=~/.local/share/newsboatimg
	
	curl -s --output - $(ntter $1) > $file && sxiv -b $file
fi
