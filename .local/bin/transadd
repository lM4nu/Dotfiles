#!/bin/sh

pidof transmission-daemon >/dev/null || (transmission-daemon && notify-send "Starting transmission daemon..." && sleep 3 )

fileorlink=$(echo $@ | sed "s/^file:\/\///" )

tremc "$fileorlink" && notify-send "🔽 Torrent added."
