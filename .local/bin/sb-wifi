#!/bin/sh

case $BLOCK_BUTTON in
	1) notify-send "$(nmcli dev wifi list | sed -E "/^IN-USE/ d;s/^.*:\w{2}\s{2}//g;s/\s*Infra.*s\s//g;s/\s{2}.*$/%/g")";;
	3) $TERMINAL -e $EDITOR $0;;
esac

nmcli dev wifi list | grep -E '^\*' | sed -E "s/.*s\s{2}//;s/\s+.*$/%/"
