#!/bin/sh

case $BLOCK_BUTTON in
	1) setsid "$TERMINAL" -e less -Srf ~/.local/share/btcchart && exit ;;
	3) "$TERMINAL" -e "$EDITOR" "$0"
esac

btcprice="$(curl -sf rate.sx/1btc)"

[ -z "$btcprice" ] && exit 1

printf " %0.2f" "$btcprice"

curl -sf rate.sx/btc@2w > ~/.local/share/btcchart
