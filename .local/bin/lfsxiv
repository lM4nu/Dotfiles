#!/bin/sh
file="$(basename "$1")"
files=$(ls -b . | grep "\.jpg$\|\.png$\|\.jpeg$\|\.gif$\|\.JPG$\|\.PNG$\|\.JPEG$\|\.GIF")
counter=0

while read i; do
	counter=$((counter + 1))
	if [ "$i" = "$file" ]; then
		pos=$counter
		break
	fi
done <<< $files

echo $files | xargs sxiv -n $pos
