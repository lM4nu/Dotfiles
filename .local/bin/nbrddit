#!/bin/sh
if [[ "$1" =~ .*\.gif ]]
then
	notify-send "Opening in mpv" && mpv "$1" && exit
fi

if [[ "$1" =~ ^.*i\.redd.*$ ]]
then
	nbimg "$1"
else
	if [[ "$1" =~ ^.*gallery.*$ ]]
	then
		rdditgal "$1"
	else
		notify-send "Opening in mpv" && mpv "$1"
	fi
fi
