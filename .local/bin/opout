#!/bin/sh

basename="${1%.*}"

case "${*}" in
	*.tex|*.[rR]md|*.mom|*.[0-9]|*.md) setsid -f xdg-open "$basename".pdf >/dev/null 2>&1 ;;
	*.html) setsid -f "$BROWSER" "$basename".html >/dev/null 2>&1 ;;
esac
