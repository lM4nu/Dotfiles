#!/bin/sh

(pidof transmission-daemon >/dev/null && notify-send "Stopping transmission-daemon" && transmission-remote --exit >/dev/null && sleep 5 && pkill -RTMIN+3 dwmblocks) || (transmission-daemon && notify-send "Starting transmission daemon..." && sleep 3 && pkill -RTMIN+2 dwmblocks )
