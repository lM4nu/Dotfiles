#!/bin/sh

choice="$(printf "Clipboard\nBrowser" | dmenu -i -p "Send to: ")"

case "$choice" in
	Clipboard) printf "%s" "$1" | xsel -bi ;;
	Browser) setsid -f "$BROWSER" --incognito "$1" > /dev/null 2>&1 ;;
	*) exit 0 ;;
esac
