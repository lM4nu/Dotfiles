#!/bin/sh

file="${XDG_CACHE_HOME:-$HOME/.cache}/newsboat/nbimg.jpg"

if [[ "$1" =~ https://imgur.com/.* ]]
then
	formatted=$(echo $1.jpg | sed "s/\/i/\/i\.i/")
else
	formatted=$(formatter "$1")
fi

wget -O "$file" "$formatted" 2> /dev/null

sxiv -b $file && rm $file
